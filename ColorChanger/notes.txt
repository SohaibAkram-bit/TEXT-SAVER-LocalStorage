In JavaScript, a method is a function that is associated with an object. It is a property of an object whose value is a function. 
Math.floor() in JavaScript is a static method of the built-in Math object that is used to round a number down to the nearest whole integer. It always returns the largest integer that is less than or equal to the given number. 
In JavaScript, Math.ceil() is a static method of the built-in Math object used to round a number up to the nearest integer.
The Math.random() static method in JavaScript is used to generate a floating-point, pseudo-random number.(range 0, 1)
The Math.random() static method in JavaScript is used to generate a floating-point, pseudo-random number.pseudo random because it is generated by a deterministic algorithm.
"JSON.stringify() is used to convert JavaScript data into a string format so it can be sent between clients or to a server. JSON.parse() is used to convert that string back into a JavaScript object that the environment (JavaScript) can understand and use."
There is always a way to read a code